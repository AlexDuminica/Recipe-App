import Layout from "../components/Layout"

export const Cooking = () => {

    return (
        <Layout>
            <h2>Cooking page</h2>
            <p>Add a container that will hold some recipes that the users adds to "Cooking page"</p>
            <p>Then, a different component below this container that will show cooking instructions when clicking on one of the recipes above</p>
        </Layout>
    )
}